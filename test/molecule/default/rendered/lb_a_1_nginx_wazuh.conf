stream {
    upstream master {
                                            server 192.168.133.22:1515;
                                            }

    upstream mycluster {
                server 192.168.133.22:1514;
                server 192.168.133.26:1514;
            }

    server {
        listen 1515;
        proxy_pass master;
    }

    server {
        listen 1514;
        proxy_pass mycluster;
    }
}

# DEBUG: Rendering for lb_a_1
# DEBUG: group_names = ['loadbalancer', 'site_a_loadbalancer']
# DEBUG: site_prefix = site_a
# DEBUG: server_group = site_a_server
# DEBUG: master node = server_a_1 # DEBUG: all servers = ['server_a_1', 'server_a_2']
